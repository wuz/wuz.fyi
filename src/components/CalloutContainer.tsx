import React from 'react';
import './CalloutContainer.scss';

const CalloutContainer = ({ children }) => (
  <section className="CalloutContainer">{children}</section>
);

export default CalloutContainer;
